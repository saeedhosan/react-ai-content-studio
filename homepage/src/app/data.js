const data = window.app_data;
export const app_data = {
  /**
   * faq
   */
  faqs: data?.faqs || [],
  /**
   * features
   */
  features: data?.features || [],

  /**
   * templates
   */
  templates: data?.templates || [],
};
