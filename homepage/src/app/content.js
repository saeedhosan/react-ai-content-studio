const app_content = window.app_content;
export const textcontent = {
  herosection: app_content?.hero_section,
  featuresSection: app_content?.features_section,
  featuresTemplate: app_content?.template_section,
  pricing_section: app_content?.pricing_section,
  contact_section: app_content?.contact_section,
  faqs_section: app_content?.faqs_section,
  footer_section: app_content?.footer_section,
};
