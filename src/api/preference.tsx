type TPreference = {
  round: boolean;
  colors: {
    primary: string;
  };
};

const preference: TPreference = {
  round: true,
  colors: {
    primary: "#407BFF",
  },
};

export default preference;
